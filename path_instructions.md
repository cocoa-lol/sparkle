# Windows
1. Add src/ to the local user path (Ex: C:\Users\{user}\Documents\sparkle\src)
2. Run the command 'sparkle'

# Linux - MacOS
1. Copy src/sparkle.py to ~/.local/bin/ and rename sparkle.py to sparkle
```bash 
mv src/sparkle.py src/sparkle && mv src/sparkle ~/.local/bin/sparkle
```